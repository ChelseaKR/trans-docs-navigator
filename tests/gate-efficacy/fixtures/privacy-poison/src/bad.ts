// Poison fixture: a direct identity field logged — the exact harm privacy-lint.ts's
// log-call scan exists to catch.
export function debugDump(current_legal_name: string): void {
  console.log("debugging intake", current_legal_name);
}
